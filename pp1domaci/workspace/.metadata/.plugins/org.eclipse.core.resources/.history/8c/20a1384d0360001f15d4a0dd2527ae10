// Test301

program test301

    const int nula = 0;
    const int jedan = 1;
    const int pet = 5;

    int niz[], niz2[];
    char nizch[];
    
{
    void main()    
        int bodovi, a;
        bool bt;
    {
        
        bodovi = -bodovi + a;
		bodovi = bodovi * 3;
        bodovi = -bodovi + (pet * a / 2 - jedan) * bodovi - (3 % 2 + 3 * 2 - 3); 
        
    }
}